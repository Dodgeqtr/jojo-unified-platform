/**
 * ثوابت مشتركة للواجهة (Web).
 * DEFAULT_ORG_ID: معرف منظمة "Jojo Main" الافتراضية في قاعدة البيانات
 * (تم إنشاؤها عبر database/migrations/002_seed_data.sql).
 *
 * ⚠️ مؤقت: عند إضافة نظام مصادقة كامل (auth)، يجب استبدال هذا
 * بمعرّف المنظمة الخاص بالمستخدم المسجّل دخوله.
 */
export const DEFAULT_ORG_ID = "f4146195-4fb9-457b-8155-a3227a563353";
