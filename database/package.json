{
  "name": "@jojo/database",
  "version": "1.0.0",
  "private": true,
  "description": "أدوات Migration و Seed لقاعدة بيانات Jojo Unified Platform",
  "scripts": {
    "migrate": "node migrate.js",
    "seed": "node migrate.js --seed-only"
  },
  "dependencies": {
    "pg": "^8.11.0",
    "dotenv": "^16.0.0"
  }
}
